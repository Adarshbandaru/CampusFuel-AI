/**
 * index.ts — Storage barrel export
 * Import from here for convenience: import { logStorage, userStorage } from '@/src/storage';
 */

export * as logStorage  from './logStorage';
export * as userStorage from './userStorage';
export * as syncService from './syncService';
